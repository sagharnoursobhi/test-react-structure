import React, { Component } from 'react';

class Unmount extends Component {
    render() { 
        console.log('the component is about to be unmount(deleting)')
        return (  );
    }
}
 
export default Unmount;